# 🎧 clipear

在 Windows 电脑上复制文章文字，一键推送到 iPhone，AirPods 自动朗读，全程无需触碰手机。

---

## 工作原理

```
Windows 复制文章文字
       ↓
  Ctrl+Shift+S
       ↓
清洗文本（去图片/链接/广告词等）
       ↓
按 1000 字分段
       ↓
通过 邮件 推送到 iPhone
       ↓
iOS 快捷指令 监控邮件自动朗读 🎧
```

- **文本清洗**：自动去除图片占位符、裸 URL、Markdown 标记、广告推广语句、全角空格等噪音
- **智能分段**：按句号边界切割，每段约 1000 字，避免通知被 iOS 截断
- **动态间隔**：根据每段字数估算朗读时长，朗读完毕后再发下一段，保证顺序
- **随时中断**：按 `Ctrl+Shift+E` 立即停止当前文章的推送

---

## 环境要求

- Windows 10 / 11
- Python 3.9+
- iPhone（iOS 14+）
- AirPods 或其他蓝牙耳机（可选，有耳机时朗读体验更佳）

---

## 部署步骤

### 第一步：iPhone 配置自动朗读邮件快捷指令

1.新建 自动化
   收到 特定发件人 + 特定主题 时自动运行
2.接受 邮件 主题 + 正文，文本: 收到邮件 "主题", 内容为 "正文"
3.朗读 "文本"

### 第二步：Windows 安装依赖

```bash
pip install -r requirements.txt
```

> ⚠️ `keyboard` 库监听全局热键需要**管理员权限**，请以管理员身份运行终端。

### 第四步：填写配置文件
  发件人、收件人、邮箱密码、日志文件位置
  每段分隔大小
其他参数保持默认即可，根据需要再调整（详见下方配置说明）。

### 第五步：启动程序

以**管理员身份**打开 PowerShell 或命令提示符，进入项目目录后运行：

```bash
python iphone_reader.py
```
无命令行窗口运行
```bash
pythonw iphone_reader.py
```
看到以下输出说明启动成功：


## 日常使用

| 步骤 | 操作 |
|------|------|
| ① | 在浏览器 / 微信 / 读书 App 中，**全选并复制**文章正文 |
| ② | 按 **`Ctrl+Shift+S`** 触发推送 |
| ③ | iPhone 收到通知，AirPods 自动开始朗读 🎧 |
| ④ | 中途想停止，按 **`Ctrl+Shift+E`** 即可 |

> **提示**：复制前不需要做任何清理，程序会自动去除图片、链接、广告等内容。


## 废弃方案
**webhook + iphone APP Bark**
  无法朗读长文本